#include<iostream>
#include<string.h>
using namespace std;

class  Dictonary{
        public:
                 Dictonary(){
                }

                string movie;
                int rating;

                string *NameArray;
                int *RatingArray;

                int count;

                void Input(){
                         NameArray=new string[100];
                         RatingArray=new int[100];
                        int i=0;

                        while(1){

                                cin>>movie;
                                if(movie=="THE-END"){
                                        break;
   }

                                cin>>rating;
                                NameArray[i]=movie;
                                RatingArray[i]=rating;

                                i++;
                                count++;

                        }

                        string tempName;
                        int tempRating;

                        for(int j=0;j<count;j++){
                                int k=j;
                                while(k<count){
                                        if( NameArray[j]==NameArray[k+1]){

                                                tempName=NameArray[j]+NameArray[k+1];

                                        }
                                        k++;
                                }
                        }

                }


     void Rating(){

                        int max=0;
                        for(int j=0;j<4;j++){

                                        if(RatingArray[max]<RatingArray[j]){
                                        max=j;

                                }
                        }

                        cout<<"Output :"<<endl; 
                        cout<<movieNameArray[max]<<" "<<movieRatingArray[max];
                }
};

int main(){
         Dictonary d;
        md.Input();
        md.Rating();
}
